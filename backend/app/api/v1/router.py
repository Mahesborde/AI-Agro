"""
AgroMind AI - API v1 Master Router

Aggregates all feature endpoints for API version 1.
"""

from fastapi import APIRouter

api_router = APIRouter()

# Future endpoints (e.g., crop-recommendation, yield-prediction, price-prediction)
# will be registered here as feature endpoints are developed in Phase 2+.
